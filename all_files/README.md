# All Files Directory

This directory contains all shared EnergyPlus sample models (.idf), weather files (.epw), schema files (.idd), and target dataset files (.csv) used by both EnergyPlus-MCP and epMCP servers.
